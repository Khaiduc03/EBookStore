import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    viewCircle: {
        flexDirection:'row',
        paddingVertical:20,
    },
    

});

export default styles;
