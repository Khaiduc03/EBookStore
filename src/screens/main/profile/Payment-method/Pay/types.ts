import { ReactElement } from 'react';
import { ViewProps } from 'react-native';

export type PayProps = {
    title?: string;
    icGoogle?: boolean;
    icZaloPay?: boolean;
    icPaypal?: boolean;

};
