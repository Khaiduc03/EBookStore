import { ReactElement } from 'react';
import { ViewProps } from 'react-native';

export type Circlepros = {
    istitle?: string,
    title?: string,
    icEdit?: boolean,
    icContinue?: boolean,
    user?:String,
    email?:String,

};