import { ReactElement } from 'react';
import { ViewProps } from 'react-native';

export type SquaresCustomProps = {
    avatarDummy?: boolean,
    name?: string,
    title?: string,
    button?: boolean,
    textButton?: string,
};
