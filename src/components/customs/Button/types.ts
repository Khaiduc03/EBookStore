import { ReactElement } from 'react';
import { ViewProps } from 'react-native';

export type ButtonBigpros = {
    title?: string;
    istitle?: boolean;
};
export type ButtonSmallpros = {
    title?: string;
    istitle?: boolean;
};
export type ButtonLongpros = {
    icon_Service?: boolean;
    title?: string;
    istitle?: boolean;
    ButtonColor?: boolean;
    icon_Facebook?: boolean;
    icon_Website?: boolean;
};